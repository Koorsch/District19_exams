---
import "../css/global.css";
const {sectionImg1,sectionImg2,sectionAlt,imgVariant,imgType, sectionVariant, sectionBackground} = Astro.props;
---

<section data-variant={sectionBackground}>
    <div data-variant={sectionVariant}
    >
       
        <div >
            <h2>Vælg pakke</h2>
            <div data-variant={imgVariant} >
                <img loading="lazy" class={imgType} src={`../media/img/${sectionImg1}`} alt={sectionAlt}>
            </div>
            <div data-variant={imgVariant} >
                <img loading="lazy" class={imgType} src={`../media/img/${sectionImg2}`} alt={sectionAlt}>
            </div>
        </div>
        
    </div>
</section>







<style>
    img {
    width: 100%;
    height: auto;
       
      
    }
    h2 {
        text-transform: uppercase;
       font-size: 1.9vw;
       font-weight: 800;
       color: #556146;
       white-space: pre-line;
		text-wrap:balance;

    }

   p {
       margin-top: 5%;
       margin-bottom: 5%;
       font-size: 1.5vw;
       line-height: 1.6;
       font-weight: 500;
      
       color: #556146;
   }
[data-variant] {
    display: grid;
    margin-left: 10%;
        margin-right: 10%;
        margin-bottom: 5%; 
}

[data-variant="2col"] {
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    justify-items: center;
}
</style>